"""The Uro-facing half of IRONWAKE: world seeding (setup.py), the declarative rule pack and its
refusal log (rules.py), the Chronicler write path (chronicle.py), library read-backs (reads.py),
and the two posture backends (uro.py). Everything here CONSUMES uro_core; nothing modifies it."""
