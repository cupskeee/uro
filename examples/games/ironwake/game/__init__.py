"""IRONWAKE's own game logic — the grid skirmish engine, unit tables, scenarios, and the
company roster. NO Uro imports anywhere in this package: Uro is not the combat engine, not the
dice, not the grid (system_prompt.md section 1). The only Uro-facing artifact this package
produces is the battle report that world/chronicle.py turns into an OutcomeBundle."""
