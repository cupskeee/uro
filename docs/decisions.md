# Decisions

ADR-lite log. Newest at the bottom; reversals get a new entry pointing at the old one, never an edit. Entries D-1…D-15 record the 2026-07-04 scoping session (owner feedback on the initial deep-research report + clarifying Q&A); D-16–D-17 the same-day follow-up; D-18–D-19 the same-day docs-hardening pass; D-20–D-24 the roast follow-up (adversarial self-review + owner choices); D-25 the 2026-07-05 federation brainstorm; D-26–D-27 the 2026-07-05 cold-reader review (18 findings from an independent LLM reviewer, verified by a fan-out workflow; 15 produced fixes, 3 were rejected/already-answered — F8 PC-ness already branch-scoped, F12 "swappable" was always storage-scoped, F13 → OQ-13). Most review fixes were doc clarifications; only these two were genuine new decisions.

| # | Decision | Rationale | Alternatives rejected |
|---|---|---|---|
| D-1 | **Engine, not platform.** Uro is a headless engine PoC; platforms/games are consumers. git:GitHub is the governing analogy. | Owner's core correction to the research report. Keeps scope survivable solo and the product genuinely reusable. | Consumer platform (the report's framing) |
| D-2 | **Community & creator-UI features are out of the engine.** Engine ships primitives instead: export packs, chronicle projection, world-pack format, dry-run. | Immature engine + platform features = death by scope. | Building sharing/library/ratings/forums/builder-UI into the engine |
| D-3 | **World state is versioned and branch-anywhere forkable** (event sourcing + snapshots + branch refs). | Owner's meteor scenarios require it; it *is* the engine's unique power. Chosen over endings-only or linear-continue in Q&A. | Branch-from-endings-only; linear saves |
| D-4 | **Nothing is permanently static.** World layers are slow- vs fast-changing; physical changes (craters) are ordinary events. | Resolves the report's "static substrate" contradiction with the owner's aftermath scenarios. | Immutable geography layer |
| D-5 | **Content-agnostic engine; content settings live on the world manifest.** No engine moderation/safety stages. Data privacy IS engine-owned. Capability probes make model-vs-world compatibility testable instead. | Owner decision; git-like neutrality. Providers and platforms own policy anyway. | Report's Safe/Moderate/Mature per-user filters and moderation pipeline |
| D-6 | **Creator tooling = file formats + CLI** (validate, probe, dry-run). Prompt template packs at world level, distributable and overridable. No module/scripting system yet. | Owner pushback: UI and module system are too much for an immature engine; prompts-at-world-level per owner. | World-builder UI; pack scripting; per-deployment-only prompts |
| D-7 | **Python 3.12 + FastAPI + Pydantic v2.** | Q&A choice. Richest LLM ecosystem; schema-heavy domain fits Pydantic. C++/C# deferred as maintainability risk for MVP (owner). | TypeScript/Node; C++/C# |
| D-8 | **Deliverable = core library + thin server + CLI reference client.** Hexagonal: core imports no frameworks/drivers. No frontend beyond the CLI, ever, in this repo. | Q&A choice. Embeddable AND servable; CLI makes the engine playable/testable without a client project. | Server-only; library-only |
| D-9 | **Single-player first, multiplayer-shaped:** campaign≠session, participant IDs everywhere, `TurnArbiter` port with `SoloArbiter`. Lobbies/arbitration deferred (OQ-7). | Owner: SP MVP, architecturally prepared for MP. | Building lobbies now; assuming party-of-one in data model |
| D-10 | **Pluggable ruleset interface + built-in "Uro Basic"** (original minimal d20). Rulesets are deterministic code, no LLM calls; affordances couple them to the planner. Turns exist only in encounter mode. | Q&A choice; owner: turn-based only in combat, free-roam turnless. | Hardcoded 5e-like; no mechanics in engine |
| D-11 | **LLM: role-based routing; adapters `openai_compat` + `anthropic` hand-rolled; Ollama via openai_compat.** LiteLLM not adopted for now (need tight streaming/structured-output control; two adapters cover the field). Revisit if the adapter zoo grows. | Owner requires multi-provider incl. self-hosted; structured output is load-bearing for state integrity. | LiteLLM as universal adapter; single hardcoded provider |
| D-12 | **OAuth-via-Codex and OAuth-via-Claude-Code shipped as experimental, off-by-default auth strategies,** documented as ToS gray-zone; API-key and Ollama are the reliable paths. | Owner asked for them (PoC); honesty about contract risk belongs in the docs. | Omitting them; presenting them as supported-equal paths |
| D-13 | **Postgres-only storage for MVP** (pgvector for vectors, edge table + CTEs for graph), behind `EventStore`/`VectorIndex`/`GraphQueries` ports. In-process bus + outbox; NATS later; Kafka rejected. Local dev = Docker Postgres (SQLite mode rejected for parity). | Owner wants "versatile, local + live" — one system that runs identically both places beats three specialized systems on day one. Ports keep Qdrant/Neo4j as swaps, not rewrites. | Neo4j+Pinecone+Postgres trio from day one; SQLite local mode; Kafka |
| D-14 | **No engine-side cost governance; mandatory usage metering** (tokens/latency per stage) from Phase 0. No fine-tuning by the engine; fine-tuned models connect as ordinary endpoints. Report's team/budget/quarter planning disregarded (solo PoC). | Owner: engine shouldn't care about cost but must be optimized; unsure about fine-tuning value — so make it a consumer choice. | Budget caps in engine; engine-managed fine-tunes; report's staffing plan |
| D-15 | **Working title "Uro"** (from folder name), explicitly provisional (OQ-11). Docs supersede the initial deep-research report (since removed from the repo before going public; these docs are the source of truth). | Need a name to write docs; renaming later is grep-cheap. | Bikeshedding now |
| D-16 | **OAuth-via-Codex and OAuth-via-Claude-Code adapters removed from scope** (reverses D-12). MVP auth = API key + no-auth local endpoints; the auth-strategy interface remains the seam if this is ever reconsidered. | Owner decision on review: not worth the ToS gray-zone and breakage risk for the PoC; `openai_compat` + Ollama already cover the connectivity goal. | Keeping them as off-by-default experimental (D-12) |
| D-17 | **Name confirmed: "Uro Engine"** (settles OQ-11, finalizes D-15). | Owner decision. | — |
| D-18 | **Dev tooling:** uv workspace monorepo; ruff + mypy (strict core ring) + import-linter enforcing the hexagonal rule in CI; pytest with recorded-provider fixtures (no live LLM calls in CI); **asyncpg + hand-written SQL + forward-only numbered migrations, no ORM**; ULID ids; `uro.toml` config with env-first precedence. Detail: `14-development-guide.md`. | Solo-PoC-sized: fastest honest toolchain; raw SQL because the `07` DDL *is* the interface and queries are simple; import-linter turns D-8's architecture rule from convention into a CI failure. | Poetry/pip-tools; SQLAlchemy+Alembic; LLM calls in CI |
| D-19 | **Contract-first discipline for the moving parts:** event types exist only in `12-event-catalog.md` (with emitter whitelists), pipeline data shapes only in `13-contracts.md`; both are versioned living specs changed in the same commit as code. Extractor is whitelist-fenced: mechanical/lethal/slow-layer events are structurally impossible for the LLM to emit. | "Living document" (owner) must not mean "undocumented"; the whitelist is the load-bearing hallucination defense, so it's a spec, not a habit. | Inline event invention; prose-only stage descriptions |
| D-20 | **Roast fixes (adversarial self-review):** (a) **trust model** — evidence hierarchy; player text is never evidence; PC assertions are testimony; `truth=true` needs narrator provenance (`13`); (b) **entity resolution** before any `*Created`, plus `EntityAliasAdded`/`EntityMerged` with merge-map semantics (`12`); (c) **repair loop rewritten** — replanning exists only pre-narration (plan validation); post-stream validation is downgrade-or-drop (`13`); (d) **fork storage = copy-on-fork** projections + content-hash-shared embeddings, stated instead of hand-waved (`07`); (e) **thesis validation reinstated** — ablation experiment with a kill criterion + continuous fact-consistency metric (`10`). | The docs were confident where easy and vague where hard; these five would have bitten in code. Notably (a): the emitter whitelist fenced event *types* but not *content* — prompt injection could previously talk the world into truth. | Discovering each in Phase 1–2 the expensive way |
| D-21 | **Mechanics gating = triggers + consequence gating.** Affordances declare mandatory trigger categories enforced deterministically at plan validation; independently, protected-state proposals (item transfers to party, `truth=true`, faction edges, T2+ changes) require mechanics backing at commit or get downgraded. | Owner choice. Defense at the door *and* the till — the trigger list will have gaps, and consequence gating catches what leaks; also narrows the injection surface. | Plan-time triggers only; consequence-only (gaslighting risk); dedicated referee pass (+1 call/beat) |
| D-22 | **Time = absolute day counter + segment on every event; the world pack's `[calendar]` config derives years & seasons arithmetically.** (Named narrative eras are event-driven History-layer concepts, not arithmetic — corrected per review F17.) | Owner choice. Envelope unchanged; "last season" and `simulate_years=200` become arithmetic instead of contradictions; fantasy calendars are world flavor, not schema. | Full structured dates (heavier envelope, calendar changes = migrations); opaque ticks (time-skips become vibes) |
| D-23 | **Player UX: free-text intent is canonical; beats may return planner-emitted `suggestions[]` (2–4, affordance-grounded) that clients render or ignore.** CLI shows them dimmed. | Owner choice. Restores the report's `options[]` idea as a hint channel without constraining play; zero extra LLM calls; gives platforms a controller-friendly hook. | Free-text only; full gamebook choice mode (requires suggestion completeness guarantees) |
| D-24 | **Probe scoring via a `judge` LLM role** with per-probe rubrics, refusal-heuristics fallback, and raw transcripts attached to every report; prefer judge ≠ judged; probes relabeled a *best-effort compatibility signal*, not certification. | Owner choice. Fits the existing role system; fixes the docs overselling probes as a cornerstone while OQ-9 admitted the scoring was heuristics + eyeballs. | Heuristics-only relabel; human-only grading |
| D-25 | **Two integration postures: GM mode (Uro owns the loop) and Chronicler mode (external game owns its domain; Uro is the world-memory/consequence layer).** Chronicler *doors* built now: external trust tier (`13`), emitter class E (`12`), async encounter completion (`06`, Phase 3); a tiny proof in Phase 5 (outcome-bundle endpoint + toy auto-battler + the war-story acceptance test with witness-scoped beliefs). Full ingestion contract deferred (OQ-12). Engine identity restated: external games answer "can I hit it, for how much?"; Uro answers "who knows what, what evidence supports it, how does new information change the story?" (owner's formulation). | Owner-driven brainstorm: tactics/4X/real-time games shouldn't run combat through Uro, but Uro wrapped around them turns battle logs into living narrative (feats → witness beliefs → rumors). Doors are cheap now and retrofit-expensive once the whitelist/trust model hardens; the full contract has no real consumer yet. Names chosen by owner (GM/Chronicler; "Chronicler" ties to the existing Chronicle concept). | Uro-runs-everything purism; full federation API now; names "Embedded/Federated" (rejected by owner), "Author/Observer", "Native/Sidecar" |
| D-26 | **NPC/monster action selection in encounters is deterministic ruleset code** — a `npc_action(state, actor_id, rng)` port method (`06`), not an LLM planner call. The pipeline drives the initiative loop; PC turns take the player's action, NPC turns take `npc_action`'s; both run through `resolve_action`. | Cold-reader F6: the port listed/resolved actions but had no *chooser*, leaving monster AI homeless. The deciding constraint is Phase 3's seeded-RNG + recorded-replay + dry-run requirement (`10`), which a live LLM chooser would break — the "no LLM in rulesets" rule alone wouldn't have forbidden a planner-side chooser. Elaborates D-10. | Planner/LLM-driven NPC combat choices (breaks replay determinism); leaving it unspecified |
| D-27 | **The import-linter contract bans the concrete provider/ruleset impls (`uro_core.providers.adapters`, `uro_core.rulesets.uro_basic`), not just leaf libraries** — and the package layout splits each port from its impl (`providers/` vs `providers/adapters/`, `rulesets/` vs `rulesets/uro_basic/`). | Cold-reader F4: the hexagonal CI rule banned `fastapi|asyncpg|httpx|uro_core.adapters` but the provider port + adapters lived in one package, so the core ring could import concrete httpx-using adapters uncaught; worse, the pure-Python ruleset built-in pulls in no bannable library, so nothing structurally stopped an engine importing it. Refines D-8/D-18; applied to the Phase-0 scaffold and verified KEPT before it could harden. | Leaf-library bans only (incidental, evaporates when an adapter's deps change); banning the port packages wholesale (breaks legitimate port access) |
| D-28 | **The LLM *planner* stage is deferred within Phase 1; Phase 1 does deterministic entity-linking for recall instead.** Roadmap Phase 1 listed "planner and extractor stages live"; the extractor + validation gauntlet ship in Phase 1, but the planner does not. | An LLM planner mainly classifies intent and routes to *mechanics affordances* — which don't exist until the ruleset (Phase 3). For the epistemic acceptance test (lie → contradiction), recall only needs to know which known entities are on stage, which is done deterministically by name/alias matching. Building an LLM planner now adds a call, latency, and complexity for marginal Phase-1 value; it becomes load-bearing (and gets built) in Phase 3 alongside affordances (D-10, D-26). | Building a full LLM planner in Phase 1 (premature — nothing to route yet); skipping entity-linking entirely (recall would be blind to on-stage state) |
| D-29 | **Phase-3 PoC encounters AUTO-RESOLVE inside one free-roam beat** (narrows D-26 for the PoC). When a plan invokes an encounter-starting affordance, the pipeline drives the *whole* initiative loop with `npc_action` for **both** sides (D-26's deterministic chooser) and commits the entire fight — `ModeChanged`→turns/effects→`ModeChanged`→`BeatResolved` — as one commit; `encounter` mode never spans beats, and there is no interactive per-turn `encounter_action`. | Phase 3's acceptance is deterministic and player-less (seeded RNG, no live turn input); auto-resolution proves the encounter mechanics (initiative, effects→events, consequences) end-to-end without building the turn-as-beat interaction loop + `encounter_action` transport (a Phase 5 session-model concern). D-26's constraint that PC turns come from the player, per-turn beats (`05`), and `encounter_action` (`06`/`08`) are the *interactive* design; they are **deferred**, not reversed — a future increment adds them behind the same ruleset port. | Building interactive turn-by-turn combat now (needs the session/turn transport, out of PoC scope); leaving the auto-resolve undocumented (contradicts D-26/`05`/`06` silently — the review's finding) |
| D-30 | **The ruleset port is game-agnostic by construction; proven by an alien second built-in (`uro_pbta`), settling OQ-13.** The port (`rulesets/base.py`) names NO game vocabulary: a `Sheet` is an opaque `dict` the ruleset validates internally; `CheckResult` carries a ruleset-declared graded `outcome` string (d20 `{failure,success}`; PbtA `{miss,partial,full}`); `CheckRequest` carries a `stat` key + opaque `difficulty` (no numeric DC); `Affordance.stat`, `Action.kind`, `Effect.kind` are open strings; `EncounterState` is an opaque ruleset subclass driven only via `current_actor`/`resolve_action`/`is_over`/`sheets`; harm reaches the timeline as the ruleset's opaque final `SheetUpdated` (never an hp-scalar event), with `ActorDied` a ruleset-agnostic lifecycle trace. **Leak report — d20 assumptions the alien ruleset forced OUT of the "generic" port into `uro_basic`:** (1) the six-ability `Ability` Literal → ruleset stat keys; (2) `Difficulty`/`DIFFICULTY_DC`/`dc_for`/`CheckRequest.dc` (the DC model) → internal to `uro_basic`; (3) `Sheet` (hp/ac/proficiency/weapon_tier/`modifier()`=`(score-10)//2`/`conscious`) → `uro_basic`'s own `Sheet`; (4) `CheckResult.success: bool` → graded `outcome`; (5) `ActionKind`/`Effect(kind=damage/death, amount:int)` Literals → open kinds + payload; (6) `Combatant.initiative` + `EncounterState.{order,turn_index,round}` → `uro_basic` private state; (7) `EncounterOutcome.casualties` (a death claim) → `out_of_fight` (ruleset decides meaning); (8) `Award.levels` (advance-by-levelling) → opaque `Award.data` (PbtA advances by marking XP on a miss); (9) the projector's `jsonb_set '{hp}'` writes in `_actor_damaged`/`_actor_died` → deleted (`_actor_damaged` handler removed; harm is opaque `SheetUpdated`). Uro Basic (d20) and Uro PbtA (2d6, harm clock, moves, no hp/ac) now run through the identical port + runner. | The engine's core claim (D-1: game-agnostic, git:GitHub) was backed by exactly one d20 ruleset; OQ-13 warned d20 assumptions had silently leaked into the "generic" contract and nobody would find out until a structurally different system was attempted. A leak-audit (7-surface fan-out) found 64; building `uro_pbta` in lockstep forced each out. A narrate-only null ruleset was explicitly rejected as the probe — it exercises no encounter/turn path, exactly where the leaks hid. | A second d20 ruleset (`srd51` — "d20 in a hat", proves nothing); keeping `Sheet`/`CheckResult` d20-shaped with per-ruleset extension hacks; a generic `HarmApplied` event (unneeded — opaque `SheetUpdated` already carries any harm model) |
| D-31 | **Multiplayer free-roam arbitration = ROUND-ROBIN turn ownership (`PartyArbiter`), behind a widened `TurnArbiter` port; settles OQ-7.** The pipeline resolves each beat as the ACTING participant's PC (`pc_for_participant`, not the campaign's single `campaign_pc` — now the solo fallback); `bind_pc` / `uro campaign join` seats a party (one PCBound per participant). The port widens from `admit -> bool` to `admit -> AdmitDecision` (`ADMITTED` / `NOT_YOUR_TURN` (hold, retry) / `REJECTED` (don't) — a bool couldn't tell these apart; `QUEUED` reserved for a future proposal-window) plus a lifecycle (`note_joined`/`note_left`/`beat_committed`). `PartyArbiter` rotates one turn token per campaign over the connected roster in join order; only the holder is admitted; the token advances when a beat commits. `SoloArbiter` = the degenerate always-admit. Turn state is SESSION-ONLY (in the arbiter), NOT event-sourced — a turn token is a live-connection concern, not campaign history, so a reconnect re-forms the roster and restarts the round. `uro serve` with >1 token spins up a `PartyArbiter`; the server drives the lifecycle from the WS join/leave/commit points and broadcasts a `not_your_turn` (distinct from `intent_rejected`). | OQ-7 left free-roam arbitration genuinely open (proposal window? consensus? GM-player?). Round-robin is the simplest DETERMINISTIC coordination that is genuinely multiplayer, testable with no clock/model, and fits the port — the right PoC first arbiter. A single-player leak audit (35 findings) showed the "multiplayer-shaped" seam (D-9) had silently leaked single-player assumptions — the pipeline planned every beat as `campaign_pc` (the first PC) ignoring the submitter; `admit -> bool` had no rotation signal; `Session`/`Participant` were dead-wired — exactly as d20 leaked into the "generic" ruleset port until an alien ruleset was built (D-30). Building the `PartyArbiter` forced them out. | Proposal-window / consensus / GM-player arbiters (deferred behind the SAME port — more design + UX, no consumer yet; `QUEUED` reserved); event-sourced turn state (turns aren't history — a reconnect would replay stale turn order); keeping `admit -> bool` (can't distinguish wait-your-turn from rejected); auto-binding a PC on WS-join (deferred — `uro campaign join` pre-seats the party, avoids building a PC sheet inside the server) |
| D-33 | **Pack-authored reactive behavior = a declarative REACTION LAYER now; a sandboxed scripting port RESERVED (supersedes D-6).** A pack ships DATA (`rules.yaml`/`agendas.yaml`), never code. One trusted, first-party interpreter in the core ring (`engines/rules.py`, sibling to `history`/`actor`/`chronicler`) reads via `ProjectionQueries`, evaluates a CLOSED/pure/total/no-float/no-loop grammar (a `Condition` discriminated union: `thread_state`/`actor_tier`/`actor_is_pc`/`edge_exists`/`world_day` + `all`/`any`/`not`, node-budgeted), and RETURNS a CLOSED `Action` union (`set_thread_state`/`create_thread`/`record_rumor`/`spread_belief`/`add_edge`/`remove_edge` over rels `knows`/`at_war_with`/`allied_with`); a trusted `run_rules_gauntlet` validates them into a bounded safe event set at the Phase-8/D-32 bar (SCOPE fence to the rule's thread-stakeholders/faction-members/place-occupants; never-mint an actor; forced `truth=unknown`/`origin=module` testimony; caps; deterministic ids `m:{commit}:{rule}:{i}`); only the commit stage writes. **Because no author code runs, the from-scratch SANDBOX — the highest-risk subsystem D-6 feared — is met STRUCTURALLY** (the `Action` union is physically incapable of naming a mechanical/lethal/canon event). Two hooks: a POST-COMMIT reaction pass (`Engine.react`, fired from `_finish` AND the Chronicler outcome path — external deaths drive reactions) and a DOWNTIME agenda tick (`Engine.agenda_tick` at `time_skip`). `CausedByKind="module"` gives auditable provenance; two trusted events (`ThreadStateChanged`/`ThreadStepCompleted`) + a projector handler ship first (no migration — `proj_threads.state` pre-existed). The rule pack is carried INLINE in `WorldGenesis` (like `prompt_overrides`) so an exported→imported world stays self-contained; the `rules_api_version` pin is enforced on the `RulePack` model itself (parse + runtime + import). Determinism: `react`/`agenda_tick` run once at live-play, never on replay; effects are events; no wall-clock/no random; `react` is exception-isolated (a reaction fault never fails the already-durable trigger beat). RESERVED, not built: a `ports/module.py` sandboxed scripting tier (WASM-preferred, never RestrictedPython), refused until a computation-shaped use-case escapes the data grammar's ceiling. | D-6 deferred module/scripting as "too much for an immature engine"; the engine is now mature + live-validated and OQ-8 (mid-play thread lifecycle + off-screen agendas) was unbuildable with the three existing surfaces (RULESET=trusted mechanics; PROMPT-PACK=declarative voice; WORLDPACK=static data) — only `ThreadCreated` had a projector, no engine advanced thread state. A design panel (4 approaches × 3 judge lenses + critic) scored the declarative layer 8.67 (unanimous "strong") over WASM/DSL/Python-hooks (6.67–7.0): it closes OQ-8 at ~L effort while DISSOLVING the sandbox risk instead of building it, and keeps the RULESET(trusted)/MODULE(untrusted) trust axis. | The scripting tiers (WASM/Wasmtime — real but XL effort + native dep + authoring friction; a restricted expression-DSL — more surface than data for no current need; isolated Python via RestrictedPython — ships a known-broken sandbox tier), all deferred behind the reserved port until proven necessary; letting the DSL grow toward Turing-completeness (loops/assignment/functions/float — refused, keeps the no-parser safety); letting a module mint entities (held the line — "X appears" = pre-author X dormant + a rule that activates it; dynamic spawn is the sharpest Stage-B trigger); an append-time emitter whitelist (structural-at-source suffices; `caused_by=module` left as the future anchor) |
| D-34 | **Pack-authored NUMERIC state = an engine-owned, event-sourced COMPUTATION LAYER (bounded integer counters as typed `CounterChanged` events → `proj_counters`); the D-33 Stage-B WASM port stays RESERVED at a sharper gate. Supersedes the D-33 clause "genuine computation is refused."** The D-33 refusal-log gate FIRED: four games built on Uro (`examples/games/`, docs/18) each produced a refusal log; the union of what they needed is BOUNDED integer accumulation (counters+threshold+reset is the #1 need in all four), NOT Turing-complete computation. The decisive argument is structural: every game put its numbers in shadow game-code that does NOT ride `fork_branch`, so branching (the signature feature) stopped covering the whole game state — the two flagship features undermined each other. So numeric state is a NEW closed event `CounterChanged{scope_ref,key,to_value:int (ABSOLUTE, never delta),created_day,updated_day, caused_by=module_cause}` projected into `proj_counters` (migration 015), registered in `projector._HANDLERS` AND `_SNAPSHOT_TABLES` so it forks/replays/snapshots/exports BY CONSTRUCTION (zero new fork code) — deleting the fragile `copy.deepcopy(ledger)`. **Shipped (INC-C1+C2):** `CondCounter` (threshold), `CondCounterCompare` (cross-entity integer cross-multiply, no float), `CondCountEdges` (count a relation, RL-5); `set/adjust/reset_counter` actions; a `world` scope (unrestricted jurisdiction; the action fence still holds); `RULES_API_VERSION` 1→2 (supported set `{1,2}` — v1 packs stay valid). Gauntlet: scope-fenced, in-pass read-your-writes accumulation, clamp fail-closed at `_MAX_COUNTER`. Determinism: the ABSOLUTE value is baked, replay re-UPSERTs, integer-only, no wall-clock/random. Trust: `CounterChanged` writes ONLY `proj_counters` — structurally never a sheet/item/death/`truth=true`; reached only through the closed Action union; non-actor-scoped; rulesets never read a counter (no lethal proxy). Phase-end SYSTEM-WIDE cross-phase review (P-comp × P0..P9): 2 confirmed (one root cause) → FIXED — `adjust_counter` is the first non-idempotent read-modify-write, so a per-branch in-process `_react_lock` serializes concurrent `react()` passes (the un-arbitered Chronicler POST path + a multi-connection participant) that would otherwise lose an increment; P2 replay/P3 lethal-proxy confirmed clean. | D-33 refused accumulation to keep the sandbox structurally met + the grammar total; the refusal logs proved authors genuinely need bounded accumulation AND that the only fork-safe home for it is event-sourced engine state (shadow state breaks branching). A design panel (4 approaches × 3 judge lenses + risk critic) scored the staged Computation Layer 8.33 / the plain Ledger Tier 8.00 ("strong"), over WASM 5.33 ("weak") and a full expression DSL 6.67. The counter substrate closes the #1 refusal at ~L effort while DISSOLVING (not building) the sandbox risk, exactly as D-33 did for reactions. | The D-33 WASM/Wasmtime Stage-B tier — RE-AFFIRMED reserved behind `ports/module.py`, now at a SHARPER gate: it opens only when a NEW refusal log shows the BOUNDED primitives were tried and STRUCTURALLY (not verbosely) insufficient — transitive/multi-hop traversal, data-dependent iteration exceeding `_MAX_FANOUT`/`_MAX_NODES`, or dynamically-computed weight tables. **Deferred, staged (docs/19):** computed cross-counter arithmetic (economy formulas — the "adjust by an expression over other counters" half, OQ-1); `for_each`/`$trigger` traversal (C3); seeded `roll_table` (C4); `expire_claim` (C5); bounded cascade re-entry (C6 — touches `react()`'s single-hop invariant, needs its own review). Also rejected: float/recursion/user-functions/unbounded loops; delta-valued counter events (double-count); per-actor counters that could grind a protected king; any counter joinable to a sheet (lethal proxy); cross-process serialization now (the P7 `expected_head` deferral). |
| D-32 | **Chronicler-mode ingestion is TRUST-SCOPED (OQ-12 hardening).** `distill_outcome` fences every effect from an external `OutcomeBundle` against the existing projection ports before minting anything: (a) **protection ceiling** — a PC (`is_pc`) or a T2+ named canon actor cannot be killed/looted/first-hand-witnessed by a bundle; a protected or out-of-cast casualty **downgrades to `truth=unknown` testimony** ("X is said to have fallen"), never a committed `ActorDied`; only an unprotected (T0/T1) *declared combatant* dies. (b) **participant scope** — casualties, loot refs, feat.actor, and witnesses must be in the bundle's declared `participants`. (c) **existence + ownership** — a casualty must exist and be alive; a loot transfer requires the item to exist AND `from_ref` to be its current owner; feat.actor is entity-resolved (`canonical_name`/`find_actor_by_name`), never a raw string. (d) feats stay **`truth=unknown` testimony** (belief-propagated to scoped witnesses), never protected canon. (e) **anti-abuse** — `OutcomeBundle` list sizes are capped; claim ids are DETERMINISTIC in (encounter_id, index) so a replay upserts the same rows (idempotent — no double kills/loot/rumors), backed by an ownership-guard on the `_item_transferred` projector. | Phase-5's toy proof (D-25) opened the Chronicler door but left ingestion of untrusted input as a documented gap: docs/13 claimed deaths/transfers commit only "within the declared domain" and OQ-12 claimed feats "pass the gauntlet", but the code enforced NEITHER — an adversarial audit (5 vectors) found 25 real exploits, incl. a single authed POST killing or looting ANY actor (a PC, a T3 king) never in the fight. Closing them makes the code match its own contract and the D-20 trust hierarchy (protected canon needs backing). Deterministic, no LLM; the protection ceiling contains the DAMAGE without a full contract. | A persisted parked-encounter registry as the non-circular root gate (deferred — "the full contract waits for a real external game", OQ-12; the ceiling already contains the damage); blanket tier≥2-death protection (would block a legit defeated mook like the warband champion — instead T0/T1 combatants die, T2+/PC downgrade to testimony); emitter-whitelist enforcement at `_append` (deferred — the LLM path is already gauntlet-fenced and `distill_outcome` emits a fixed safe event set); dropping rumors that contradict canon (testimony is ALLOWED to be false — it is not canon) |
| D-35 | **Uro's public identity is *a world-state engine* on its own terms; retire the git:GitHub analogy from all outward-facing framing (refines D-1).** D-1 recorded "git:GitHub is the governing analogy" — a genuine comprehension scaffold, but as *public identity* it read as derivative ("git, but for RPG worlds") and implied Uro is "for games." Outward copy (README, `CLAUDE.md`, the social preview, docs 00/01/03/08/10/glossary) now leads with Uro as **a world-state engine: the persistent, versioned, canonical record of a living fictional world** — tracking not only what is *true* but *who believes it*, with forkable timelines — described with no external-product comparison. Tagline: "Worlds that remember. Timelines that fork." The engine/platform boundary and game-agnosticism of D-1 STAND; only the analogy-as-identity is dropped (D-1/D-30 keep their historical text — append-only). | Owner (2026-07-13): the analogy was a crutch to make the concept legible to the LLM agent; Uro should stand as its own unique thing, not be marketed as a git-for-RPG derivative. Framing chosen from a 5-angle positioning panel + judge (owner picked the category-defining option). | Editing D-1/D-30 in place; keeping the analogy in outward copy; a heavier rename/rebrand |
| D-36 | **Participant-scoped memory (B8, #7) is a caller-owned lane, NOT event-sourced; the event-sourced "participant journal" is RESERVED behind the same port on an evidence gate.** A player's out-of-world meta-knowledge (time-loop / roguelike / NG+) lives in `participant_notes(participant_id, world_ref, key)` (migration 017) behind a `ParticipantMemory` port — deliberately NOT a projection, NOT in `_SNAPSHOT_TABLES`, never touched by `fork_branch` — so fork-survival is STRUCTURAL (a fork copies only branch-keyed rows) and canon-safety is STRUCTURAL FOR DIRECT WIRING (nothing reads a note into the extractor/planner/`proj_claims`/`proj_beliefs`/belief-propagation). It surfaces only in the narrator prompt as the player's private recollection; the narration→extract echo is the same by-policy, narrator-tier-trust residual every narrator input already has (not a new hole). OQ answers: per-(world×participant) scope (`world_ref` is the knob; global reserved); surfaces to the narrator (distinct "the world does NOT know this" channel) AND is client-queryable; LOCAL to the player, NOT in the world bundle (a separate participant export is deferred); caller-supplied key + `sha256(text)` fallback (idempotent). CLI `uro codex add/list`; `Engine.remember_participant`. | Hollowloop G-1 evidenced the genre-defining need; because a note is NOT an event it can't reach canon and isn't fork-copied — "not an event" is the cleanest home. A 4-approach design panel + judge scored caller-owned (D) 8.4 over the event-sourced journal (C) 8.2 / codex-spine (A) 8.0 / scope-tagged-claims (B) 7.2. Only ONE game evidenced it → ship the primitive, reserve the heavier journal (validate-before-building, per the `entity_index`/OQ-3 precedent). | The event-sourced `ParticipantEvent` journal (C — reserved until a 2nd game needs audit/forget/confidence-decay); scope-tagged claims routed through the shared projector (B — a self-inflicted canon-leak surface for no gain); global cross-world scope; putting notes in `WorldBundle` (a spoiler/privacy leak); engine auto-harvest of what-to-remember (would dump canon/spoilers into the player lane) |
| D-37 | **A client-supplied `BeatPlan` (B9, #8) is a first-class, TRUSTED in-process library input: `Engine.run_beat/run_beat_stream/preview_beat(..., plan=…)` drives the planner→mechanics gate deterministically, skipping the LLM planner — but the plan is FENCED exactly like an LLM plan.** A supplied plan runs through the SAME `validate_plan` (affordance fence + D-21 trigger coverage over the ruleset's DECLARED vocab; unknown affordance → `PlannerError`) and the SAME downstream paths (`_mechanics` free-roam checks, `encounter_trigger`/`_resolve_encounter` for an attack — including the `is_pc` PvP refusal → free-roam fallback), so a `plan=` beat can do nothing an LLM plan for the same recall couldn't. `BeatResult` gains `check_traces: list[str]` (per-check detail, not just the count) and — new in the phase-end review — the resolved ENCOUNTER's rounds now also surface in `check_traces` (a combat beat previously reported an empty count despite rich deterministic mechanics). Purpose: CI mechanics coverage + keyless/embedded consumers resolve checks with no model. **Trust posture (explicit):** `plan=` is an in-process library call — the caller IS the application, already able to drive the store directly, so there is no privilege boundary to defend and NO D-32 protection ceiling is applied. The D-32 ceiling remains the fence for the EXTERNAL, authed-but-untrusted Chronicler `outcome` POST (`distill_outcome`). **If `plan=` is ever exposed across a trust boundary (a REST endpoint accepting a caller's plan), it MUST first route through the protection ceiling** — this is NOT that path. MVP scope: library API only (not wired to `serve`/CLI). When no ruleset is bound or the engine is `--bare`, a supplied plan is correctly not gated (nothing to gate / ablation must run no mechanics) but now LOGS a warning instead of silently voiding it. | The CI mechanics gate had been covered per-test by hand-scripted planner-provider JSON (a stub hack); `plan=` makes the deterministic path a real API. Fencing reuse (not a new validator) keeps the supplied and LLM paths at exactly one fence. Trusting the in-process caller (vs. re-applying the D-32 ceiling) matches the hexagonal boundary: the library consumer is inside the trust boundary; the ceiling is for the network edge. Phase-end SYSTEM-WIDE review (B9 × P0..P9, 11 agents): 2 confirmed low (stale example-doc field lists; silent-drop diagnostic) + a completeness-critic catch (combat `check_traces` empty) — all fixed; the encounter path is now covered by an injected-plan test. | A second, separate validator for supplied plans (drift risk — one fence is the invariant); applying the D-32 protection ceiling to the in-process `plan=` call (wrong boundary — it would also have to gate every direct `store` write the caller can already make); wiring `plan=` to `serve`/CLI now (deferred until a network consumer needs it — and that MUST add the ceiling); a `CheckResolved` event trace / rule-based fallback planner (reserved behind #8); running a supplied plan under `--bare` (ablation integrity) |
| D-38 | **Arbiter shapes beyond round-robin (B7, #9, OQ-7): ship the SAFE half — a structurally non-canon coordination lane + a propose-then-act (`QUEUED`) arbiter + a consensus/vote arbiter — and RESERVE every shape that would cross a load-bearing seam behind the same `TurnArbiter` port.** Built (all SESSION-ONLY per D-31, zero domain events / migrations / beat-loop change, fully deterministic): (1) a **non-canon table-talk lane** — a WS `table_talk` frame handled inline → `hub.publish` only, so it can NEVER reach `run_beat`/`append_beat` (non-canon **by construction**, asserted by a "run_beat untouched" test); (2) `AdmitDecision.QUEUED` promoted reserved→**live** + `ProposalWindowArbiter` (a non-holder's intent is QUEUED → surfaced as a `proposal_opened`, not a silent `NOT_YOUR_TURN`; round-robin ring/rotation/G-17 departure fix inherited; the decided action still runs as ONE ordinary beat — `admit` stays a pure query); (3) `VoteArbiter` + an OPTIONAL `runtime_checkable VoteCoordinator` capability (NO new required port method) — a session-only tally decided by STRICT plurality once all connected roster voted (ties stay undecided), enacted as one ordinary beat. CLI `uro serve --arbiter party|proposal|vote` + `uro connect` `/say`//`/vote`. **DEFERRED behind the same port, each verified to still fit** (validate-before-building on ONE game's S1 probe): **G-8 consensual-PvP** (the only shape that would edit the P7 anti-grief invariant + effect path — highest blast radius; consent-to-*contest* wouldn't even reproduce the heist's fixed-outcome double-cross), **G-9 simultaneous/composite** (the only shape that rewrites one-intent-one-beat; a mixed combat+free-roam composite double-books the fight's canon through one fused extraction), **G-12 reactive/interrupt** (out-of-band admit during an in-flight beat = a SECOND concurrent writer, and the per-campaign `expected_head` guard is deferred — needs the concurrency story settled first), and **take_pending** (auto-promoting a winning proposal into an on-behalf-of beat). | A 4-approach design panel (port-pure / composite-substrate / consent-first / strict-gate) × 5 judge lenses + an adversarial risk-critic (14 agents) scored 7.0–7.5 and the critics dismantled each flagship claim — interrupt is NOT free (concurrent writer), a mixed composite double-books combat canon, consent-to-contest can't reproduce a fixed-outcome betrayal. The synthesis grafted the safe half. Phase-end SYSTEM-WIDE review (P-arbiter × P0..P10, 14 agents): 7 confirmed (all low) → fixed — a VoteArbiter liveness stall (a non-voter DISCONNECTING completes a round `cast_vote` never re-checks → `resolve_pending` on `note_left`), a silently-swallowed `/vote` on a non-vote arbiter (now a `vote_unsupported` frame), and this doc drift. | Building all five shapes from one consumer's wishlist; a composite-beat substrate (XL beat-loop surgery + a real double-booking hazard) on one probe; editing the anti-grief invariant (`engine.py`) for consensual PvP on one probe (a trusted in-process bypass would drop that invariant from by-construction to by-policy); event-sourcing turn/vote/proposal state (it is live-connection coordination, not campaign history — D-31); a new REQUIRED `TurnArbiter` method (`cast_vote`/`resolve_pending` are an optional `VoteCoordinator` capability); auto-promoting a proposal into an on-behalf-of beat (`take_pending`) |
| D-39 | **Session lifecycle (B10, #10, OQ-7): REFINE D-31, do NOT reverse it — durable turn ORDER derived from the already-event-sourced bind log; the turn CURSOR stays session-only; runtime tokens ship as a durable, hashed, revocable, campaign-scoped registry OFF the branch axis.** The fork argument is decisive both ways: a durable turn CURSOR modelled as an event/projection would land in `_SNAPSHOT_TABLES` and be materialized by `fork_branch`/`import_world`, seeding a what-if fork (or a re-imported world) with a frozen "it's B's turn" that has no live referent — the exact stale-roster problem D-31 refused. "Who is a PC as of commit X" IS a frozen-at-commit story fact (`proj_pcs` already forks correctly); "whose turn is it right now" is an irreducibly LIVE fact the log cannot represent. So: **G-18** — `store.pc_seats(campaign_id)` recovers ring ORDER from the PCBound/PCReleased log (walk the campaign-branch ancestry, first-ever-PCBound-per-participant, drop released, re-bind returns to seat) — NO new event; `TurnArbiter.note_joined(seats=…)` orders the ring by it AND recomputes `_turn` to PRESERVE the current holder across the re-seat (the G-17 bug class on the JOIN side). The cursor still resets to 0 on a full restart (round restarts over a now-stable order) — resume-after-crash is REFUSED (it is the fork hazard) and reserved. **G-19** — a durable `session_tokens` table (migration 018, `sha256`-at-rest, revocable) OFF the branch axis (the `participant_notes`/D-36 pattern: not a `proj_*`, not in `_HANDLERS`/`_SNAPSHOT_TABLES`, never forked/imported) behind the SINGLE `resolve_participant` choke point via a write-through `TokenRegistry` cache (sync resolve; re-hydrated at startup so a minted token survives a restart — an in-process-only registry would lock out every runtime-added player). Scoped mint folded into the authed `POST /join` (returns `{actor_id, token}`) + standalone `POST /tokens` + `/tokens/revoke`. CLI: `uro serve --arbiter` decoupled from token count (UNSET=auto solo/party; EXPLICIT binds always, so a runtime-added player gets turns), `--admin-token`, `uro token mint/revoke`. | A 4-approach design panel (derive-no-reversal / full-event-source / auth-first / strict-gate) × 5 lenses + risk-critic (14 agents): the full reversal (B) was DEMOLISHED — a durable cursor structurally can't deliver resume (an offline holder either wedges the party or is overwritten by the first reconnector) AND rides `fork_branch` nonsensically; its two good pieces (ring-from-log, durable token table) both stand WITHOUT the reversal. Phase-end SYSTEM-WIDE review (P-session × P0..P10, 13 agents): 5 confirmed → fixed — **two real auth holes**: every `--token` peer was treated as admin (self-or-admin scope vacuous → impersonation) → an explicit `--admin-token` operator tier distinct from player tokens; minted tokens authenticated server-wide (cross-campaign hijack) → tokens are now campaign-scoped, enforced on the WS play channel; plus a WS queue-leak (the `pc_seats` DB call sat before the `try/finally` — a transient failure orphaned the SessionHub queue). | The full D-31 reversal / an event-sourced turn cursor (rides `fork_branch`, can't deliver resume, reverses D-31+D-38); an in-process-only token registry (loses runtime tokens on restart → locks out added players); modelling any session/turn/token state as a `proj_*`/branch-scoped event (would fork/snapshot — the hazard); every `--token` = admin (impersonation); a server-wide (un-campaign-scoped) minted token (cross-campaign hijack); freezing arbiter kind by launch token count (a runtime player on a solo-launched server gets no turns); a `bound_seq` column now (perf, deferred — the per-join ancestry walk suffices). **Named residuals (low, deferred):** no resume-after-crash (cursor resets over a stable order); revoke blocks a NEW connect but a live WS socket survives (auth checked once before accept); no per-participant token cap / no revoke-on-`end_campaign` (durable tokens outlive their binding); cross-process cache coherence (single-process PoC). |
| D-40 | **Reaction-layer scope gains MULTI-REF forms + every dropped action is AUDITED (B11, #11; settles docs/19 OQ-2's multi-ref half).** (1) `Scope` gains plural jurisdiction fields (`threads`/`factions`/`places`) alongside the singular ones; `_scope_refs` unions members across all named entities of ONE category — a rule may span, e.g., a pact BETWEEN two factions without the blunt `world` scope (least-privilege middle ground). A `model_validator` enforces EXACTLY ONE jurisdiction (world XOR one category; empty / multi-category / world+category are rejected — an empty scope would silently drop every action). This widens JURISDICTION only; the action fence (the closed, non-canon Action union — no mint/kill/loot/`truth=true`) is untouched, so multi-ref is not a trust-escape. `RULES_API_VERSION` 2→3 (supported `{1,2,3}`; v1/v2 packs stay valid; a multi-ref pack — which a `_STRICT` older engine would reject on the unknown field — declares v3). (2) The gauntlet now returns `GauntletResult(events, drops)`: every refused action (out of scope / nonexistent thread / edge endpoint out of scope / no in-scope subjects\|witnesses / nonexistent claim / a partial subject/witness filter / the `_MAX_ACTIONS` per-pass overflow) records a `DroppedAction(rule_id, do, ref, reason)` — the dropped-action audit trail (before this, a fenced action `return []`-ed SILENTLY, so a pack author could not tell a working rule from a no-op). The gauntlet stays pure (records only); `react`/`agenda_tick` log one summary line per pass. Not canon and not an event — a diagnostic (logged live; returned for tests / a future creator-loop surface). | Sable G-3/G-12, Ironwake, Seventh G-6 all hit the single-dimension scope (the umbrella-faction hack) and the silent-vanish. Multi-ref is the least-privilege answer between one-faction and `world`. A concrete, bounded extension of a well-understood subsystem → built directly (no design pass). Phase-end SYSTEM-WIDE review (P-B11 × P0..P10, 12 agents): 6 confirmed (all low) → fixed — the `_MAX_ACTIONS` cap truncated the tail with no drop record (now audited); partial subject/witness filters were silent (now recorded); a double-log (per-action + summary → gauntlet records, caller logs); + this doc drift. | A `str \| list[str]` union on the singular fields (ambiguous YAML); a `world` wildcard as the only cross-entity option (over-broad — multi-ref is least-privilege); relaxing the ACTION fence for multi-ref (never — jurisdiction only); a committed `ReactionDropped` EVENT for the audit (canon-ish noise — drops are a pure diagnostic, not timeline state); logging drops both per-action and in an aggregate (spam). |
| D-41 | **Chronicler protected-canon ingestion (B6(b), #12, OQ-12): a TRUSTED-embedder distillation tier now (unblocks the 3 games' authored deaths); the untrusted parked-encounter registry RESERVED. Trust is which MODULE you import, never a wire flag — the outcome twin of D-37's `plan=`.** The distill body is one parameterized `_distill(store, branch, bundle, *, protect)` in the internal `uro_core/_distill_core.py`; all four protection sites (casualty-death, witness-conscript, loot from_ref, loot to_ref) route through the injected predicate. `chronicler.distill_outcome*` (unchanged signatures) inject `_is_protected` — the UNTRUSTED network path, full D-32 ceiling. `uro_core/authored.distill_authored_outcome` injects `_never_protected` — the TRUSTED in-process path a Posture-A embedder (which already holds root via `append_beat`) uses to reuse distillation with the ceiling OFF, so an authored protected (T2+/PC) death commits as real canon + fires succession pack rules via `append_and_react`. **The load-bearing fence is structural + CI-enforced:** an import-linter contract forbids `uro_server` from importing `authored`/`_distill_core` (verified to BITE), so the wire layer cannot even name the lethal path; `OutcomeBundle` is `extra='forbid'` + version-pinned (a forged `{trust:…}` field or unknown `v` is a loud 400, not a silent drop). Honest label: structural vs the WIRE; the residual (a deliberate in-core edit importing `_distill` with a false predicate) is not language-preventable, backstopped by a behavioral test + review (D-37's accepted posture). **Shipped riders:** out-of-cast casualty now DROPS (was a public rumor — Ironwake row-7 leak); loot `to_ref` protection (was from_ref only); the outcome endpoint enforces the D-39 token→campaign scope. | A 4-approach design panel (trusted-tier / parked-registry / auth-first / sanctioned-gauntlet) × 5 lenses + risk-critic (14 agents): the registry-now approach was shown to ship INERT as specced (admin tokens are static → `campaign_of`→None → the park predicate is always False) and premature (no untrusted network game exists — all 3 evidenced games are Posture-A embedders holding root); the trust-flag-kwarg approach opened a second bypass surface the grep-test missed. Synthesis grafted the module-fence trusted tier + the untrusted hardening. Phase-end SYSTEM-WIDE review (P-chronicler-trust × P0..P-comp, 12 agents): 6 confirmed (all low) → fixed — a malformed bundle 500'd instead of the claimed 400 (now caught); the Ironwake game tests + a self-check asserted the old out-of-cast rumor (reconciled to the drop); doc drift. | Building the parked-encounter registry now (premature + inert-as-specced; RESERVED with a CORRECTED design: branch-scoped EVENT-SOURCED state — not the off-axis token/notes pattern, so a pre-park fork inherits no authorization; a non-reserved column name — `cast` breaks snapshot SQL; campaign-agnostic admin auth — not `is_admin AND campaign_of==c`); a `trusted=True` kwarg on the wire-reachable function (second bypass surface — trust must be a module, not an argument); a committed `ReactionDropped`-style event for downgrades. **Named residuals (low):** the trusted tier can kill a PC-bound actor leaving an active-but-dead binding (release it yourself — a PC death is a lifecycle event, and it's reachable via `append_beat` anyway); Seventh G-21 non-lethal harm (schema v2 — needs ruleset-mediated sheet effects); game↔world time mapping; append-time emitter whitelist; whole-REST per-campaign authority. |
| D-42 | **Quantified/relational reaction triggers (RL-6, #25): a `$trigger.<field>`-aware `when`, evaluated PER matching trigger event, + a `trigger.per_event` fire quantifier. Extends D-33/D-40's reaction grammar; the trust fence is untouched (conditions are reads).** "React on the death of ANY member of faction X" was unexpressible: the old `where: {actor.member_of: …}` was accepted-but-inert, then (dogfood findings) refused loudly, leaving no way to say it. Now a condition's ENTITY-REF slots (`actor`/`src`/`dst`/`scope_ref`/`thread`; counter_compare's nested `scope_ref`s) may be `$trigger.<field>`, bound from the triggering event payload — reusing the C3 `_resolve_trigger` machinery (HOISTED from `rules_gauntlet` into `engines/rules.py`, gauntlet re-imports; no cycle) threaded into the previously payload-blind `_eval`. `when` is evaluated per matching event, so `$trigger` is a true EXISTENTIAL over the beat (the naive "bind the first event" shape misfires when a non-member dies first). Default fires the rule ONCE (first satisfying event); `per_event: true` fires once per satisfying event (count-each, e.g. `adjust_counter` per dead member — event-sourced, so it rides `fork_branch`, unlike a shadow game-code counter), each keyed by `FiredAction.event_key` for distinct idempotent ids (mirrors for_each's neighbor-index path). Fences: parse validates every `$trigger.<field>` — legal only in a ref slot of a rule (a `$trigger` in a literal slot / an agenda rule is a loud error), and the field must be a STRING-scalar field of the trigger event (a list field like `subject_refs` is rejected, not accepted-but-inert); at eval, an unbound-or-null ref fails the WHOLE `when` closed (a `not`-wrapped unbound ref can't fail open). `RULES_API_VERSION` 4→5 (v1–v4 packs byte-identical — a `$trigger`-free `when` is evaluated ONCE per beat, so their node-budget cost is unchanged). Resolves Ironwake's quantified-trigger gap-report row. | LIGHTWEIGHT design check (owner-chosen middle path — not a full panel): 3 blind proposers from different angles (author-ergonomics / engine-invariants / future-generality) all independently converged on `$trigger`-aware `when` (high confidence); a 4th adversary (opus) then KILLED the naive scope — "bind the first matching event" cannot express "ANY member died" in a multi-death beat where a non-member dies first — mandating per-event evaluation + `per_event` + whole-`when` fail-closed + a ref-slot registry. Phase-end SYSTEM-WIDE review (P-RL-6 × P0..P10, 13 agents): 8 confirmed (de-duped to 4 distinct + 2 critic; 2 medium, rest low) → all fixed — (1) a real BUDGET regression: per-event re-eval of a `$trigger`-free `when` burned the shared `_MAX_NODES` per event and could starve/drop a beat's whole reaction pass (fixed: constant `when` evaluated once); (2) a list/non-string `$trigger` field validated-but-never-matched (fixed: string-scalar parse check); (3) a present-but-null field (`BeliefChanged.learned_from`) bound as `"None"` → fail-OPEN under `not` (fixed: null = unbound); (4) a `counter_compare` literal-slot error message said "(none)"; + per_event fan-out honestly capped by `_MAX_ACTIONS` (audited) and a CHANGELOG overclaim. Verified clean: P3/Chronicler `report_outcome` runs `react` (RL-6 fires in real play); P2 per-event emissions rebuild on a fork; P5 v5 pack export/import; P9 for_each still resolves `$trigger`; the hoist keeps the hexagonal boundary. | A relational trigger `where_edge` operator (a bespoke second grammar, edge-only, composes with nothing — strictly less general than reusing the closed condition tree); binding only `_matching_trigger`'s FIRST event (the adversary's killed naive shape — misfires the existential + can't count); leaf-level fail-closed (a `not(unbound)` fails open — must be whole-`when`); a `$trigger` in for_each ACTIONS getting its own second validator (the same ref-slot walk covers `when`; action-side `$trigger` stays gauntlet-fenced). **Deferred (named):** multi-hop/transitive triggers ("a member of an ALLY of X") need `for_each` traversal in CONDITIONS (reserved); aggregate triggers ("the 3rd death this season") compose WITH a counter+threshold, not a new primitive; a `$trigger` in top-level actions (symmetric follow-on, out of scope). |
| D-43 | **Distribution posture (#15): Postgres + pgvector stays the ONE store — no second in-memory/embedded backend — and the packages ship to PyPI as OWNER-ACTIVATED plumbing, fronted by a docker-first runtime.** Concretely: (a) the base `uro-core` install is the pure engine (it imports only ports — verified: `import uro_core` and the whole core ring load with none of `asyncpg`/`pgvector`/`httpx`); the bundled adapters move behind extras (`postgres`, `llm`, `all`); `uro-cli`/`uro-server` pull both. (b) `uro` now turns an unreachable database into an actionable hint (`docker compose up -d --wait`, host port 5433) via a single `connect_store` wrapper, instead of leaking the driver's traceback. (c) `__version__` is read from installed package metadata (killed a `0.0.1`-vs-`0.1.0` drift class). (d) a manual `publish.yml` (PyPI trusted publishing, OIDC — no stored token) builds and uploads all **three** packages together (`uro-cli`→`uro-server`→`uro-core`, single-versioned); it is inert until the owner registers a per-package pending publisher (docs/14). Wheels now also carry the LICENSE + a per-package README long description. | Measuring the "frictionless install" ask against the code changed the answer: a faithful no-DB store is a full second `EngineStore` — a ~60-method union port, a projector coupled to hand-written SQL per event, plus snapshot/fork/materialize machinery — carrying a STANDING drift tax (every future event type / projection mirrored in two backends or it silently diverges). For an engine whose defining feature is pgvector semantic memory and which has no external consumers, that tax isn't justified; a SQLite backend fares no better (no pgvector equivalent → a brute-force vector scan + a maintained second dialect). Docker-first removes essentially all install friction for essentially no code and zero drift risk, and the packaging correctness (extras, license/readme, metadata version) is a real win regardless of when publishing is switched on. Trusted publishing (a module/OIDC identity, never a wire flag or a stored secret) mirrors the D-37/D-41 "trust is which thing you invoke" stance. | A full in-memory `EngineStore` (the drift tax above); a scoped play-loop-only in-memory store (a partial second code path + `NotImplementedError` cliffs); a SQLite backend (a second SQL dialect with no vector story); publishing without the extras split (a heavy base install that drags a DB driver + HTTP client onto every embedder); a stored PyPI API token (trusted publishing avoids it); auto-publish `on: release` (a failed/premature upload would surprise a plain tag push — kept manual until the owner opts in); a `ghcr.io` `uro-server` image now (needs a Dockerfile; the server is a thin shell — deferred). |

---

**Corrections (consolidation, 2026-07-09 — factual fixes to implementation-detail claims in past decisions; the DECISIONS themselves stand):**

- **D-30, leak item (9)** said the `_actor_damaged` projector handler was *deleted*. It was deleted in inc 6.1, then **restored in the Phase-6 review as a documented legacy replay-compat handler** (commit `b8a6f6b`): the CURRENT harm path is the ruleset's opaque `SheetUpdated` (the decision stands), but old d20 fight logs recorded HP only as accumulated `ActorDamaged` reductions, so the handler is retained to keep rebuild-by-replay==original. `ActorDamaged` has no live emitter (docs/12 marks it legacy). The D-30 *decision* (harm is opaque, hp/ac out of the port) is unchanged.
