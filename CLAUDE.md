# CLAUDE.md — working in this repo

Uro Engine: a headless, game-agnostic AI-RPG **engine** PoC (git-is-to-GitHub as Uro-is-to-a-game/platform). Python 3.12, `uv` workspace. Full design in `docs/` — read `docs/15-walkthroughs.md` for the map, `docs/10-roadmap.md` for status, `docs/16-honesty-ledger.md` for the honest proven/proxy/stub-only/deferred map, `docs/decisions.md` (D-1..D-33) for why things are the way they are. **The docs are authoritative; when code and docs disagree, fix one to match the other in the same commit.**

## Layout

`packages/uro-core` (all engine logic, embeddable) · `packages/uro-server` (thin FastAPI shell — scaffold only so far) · `packages/uro-cli` (`uro` reference client). Design detail: `docs/01-architecture.md`.

## Commands

```
uv sync --all-packages              # install the workspace
docker compose up -d --wait         # Postgres + pgvector on HOST PORT 5433 (not 5432)
uv run uro db migrate               # apply forward-only SQL migrations
just test                           # THE gate: ruff + ruff format --check + mypy + import-linter + pytest
just fmt                            # ruff format + --fix
uv run uro world new "Name"         # → a campaign id
uv run uro play <campaign>          # interactive; --provider stub|local|openai|anthropic ; --bare = ablation
uv run uro consistency <campaign>   # thesis proxy metric T2
```

`just test` needs Postgres up; DB-requiring tests auto-skip if it's down. **CI never makes live LLM calls** — provider-dependent tests use recorded/mock transports or the deterministic stub. No credentials or Ollama were available in the build environment, so the live-model run is pending (commands in `docs/10` Thesis section).

## Non-negotiable conventions

- **Hexagonal boundary (enforced in CI by import-linter):** the core ring — `uro_core.{domain,timeline,engines,pipeline,memory}` — imports only **ports**, never concrete adapters (`adapters/`, `providers/adapters/`, `rulesets/uro_basic`). Keep the contract KEPT.
- **Everything is an event.** State changes are typed domain events (`domain/events.py`, catalog in `docs/12`); projections are rebuildable read-models written only by the projector, in the same transaction as the commit. Never mutate a projection directly.
- **Migrations are forward-only** (numbered SQL in `adapters/postgres/migrations/`); a bad one gets a new correcting migration, never an edit.
- **Tests assert on committed events, not prose.** Structured output is validated (Pydantic); the extractor is fenced by schema + gauntlet.
- **mypy is strict on the core ring** (`domain`/`timeline`/`pipeline`); adapters lenient. Line length 100.
- **Commit messages: NO `Co-Authored-By` footer** (owner preference). Shells choke on backticks/`<>` in `-m` — write the message to a scratch file and `git commit -F`.
- **Decisions append to `docs/decisions.md`** (never edit a past decision; a reversal is a new entry pointing at the old). Settled open-questions move out of `docs/11`.

## How increments get built (the rhythm that's worked)

Build each increment as one coherent, tightly-coupled slice **directly** (don't fan out — layers interlock), run `just test` green, and **commit it**. **Review at the END of the PHASE, not after each increment** (owner directive, 2026-07-07): build all of a phase's increments first, then run ONE **multi-dimension adversarial review** via the Workflow tool (find → verify each against the ACTUAL code, default REJECTED; + a completeness-critic pass). Triage verified verdicts; fix confirmed-real, defer verifier-agreed non-issues (name them in the commit); reconcile any doc/code drift in the same commit. The review reliably finds a real edge-case bug (and doc-overclaim) per phase, outside the happy path the tests cover. Prior review scripts are under the session workflow dir.

**The phase-end review must be a SYSTEM-WIDE CROSS-PHASE seam hunt (owner directive, 2026-07-08), not just "how this phase touches its neighbors."** Enumerate the interactions between the NEW phase and EVERY prior phase (phase-N × phase-M) and check each holds — this is the rigor of the once-at-the-end cross-phase review (`217bc9c`, which caught 7 phase-interaction seams no per-phase review could see), applied at *every* phase end. The narrower "how does this phase disturb its obvious neighbors" scope misses seams: in Phase 6 it let one cross-phase path (export/import of a PbtA world, P6×P5) go untested until follow-up. So a phase-end review dimension is explicitly "the new phase × each of P0..P_{n-1}"; feed the reviewer the whole-repo subsystem map, not only the phase diff.

**Watch for overclaiming:** trace whether the code actually enforces a guarantee before writing it; prefer "by construction (structural)" vs "by policy / best-effort"; label proxies as proxies. (Reviews caught this 4× in Phase 1.)

## Status (2026-07-07)

Phase 0 + Phase 1 (state engine: recall → narrate → extract → gauntlet → commit → project; claim/belief epistemic layer; pgvector semantic memory; 4 provider adapters; thesis harness) are **code-complete**. The **thesis was validated live** via OpenAI — the ablation (`scripts/ablation.sh`, `docs/live-run.md`) showed the full engine recalling a named NPC + plot fact past the recency window while `--bare` forgot them. That run also surfaced extractor over-extraction (flavor → `truth=true`), fixed in Phase 1.5. **Live runs:** the owner's `OPENAI_API_KEY` is not visible to Claude's bash — the owner runs keyed commands, Claude analyzes results from Postgres.

**Phase 2 (the signature phase — branching timelines) is code-complete, 91 tests green, fully deterministic (no key needed). The meteor acceptance test PASSES** (`tests/test_meteor.py::test_the_meteor_test`): one played campaign ends with a player-caused `PlaceDestroyed(Vel)`, and from the SAME event log — continue (adopt the wizard PC, face the crater), new-life (fresh farmer a year later via time-skip; NPCs retell the deed as history; the wizard is now a legend/NPC), and what-if (forked pre-strike, Vel stands) all coexist, no special-casing.

- **inc 2.1 (substrate):** commit-depth, `markers`, `snapshots` + materialization-at-any-commit (nearest-snapshot + replay), copy-on-fork branching (`fork_branch` — projections rebuilt at the fork commit, memory-index rows copied, embeddings shared by content-hash), the `proj_places` projection, CLI `uro branch fork/list/mark` + `uro log`.
- **inc 2.2 (fork semantics):** PC binding (`proj_pcs`, `PCBound`/`PCReleased`, `is_pc`/`active_pcs` — PC-ness is per-branch, the same actor is a PC on one fork and an NPC on a sibling), campaign lifecycle (`start_campaign` adopt-or-fresh, `end_campaign` releases PCs + marks the fork root), deterministic time-skip (`TimeAdvanced` + honest `AdaptationApplied` header, no LLM ripple), CLI `uro campaign new/end` + `uro branch fork --time-skip-days`.

Each increment got a 5-dimension adversarial-review workflow (find→verify, default-rejected): 2.1 → 4 fixes (branch-name `UNIQUE`, `create_marker FOR UPDATE`, `PlaceStateChanged` enum bar); 2.2 → 1 fix (acceptance test now asserts an active PC binding survives a fork — guarding the snapshot `pcs` path).

**Phase 3 (mechanics) is COMPLETE — code + a phase-end holistic review, 126 tests green, deterministic (Claude builds AND verifies with a stub planner; the live planner run is the owner's, like Phase 1). The acceptance test PASSES** (`tests/test_encounter.py::test_the_acceptance_insult_to_combat_to_consequences`): a free-roam insult, then an attack triggers the pipeline's mode transition to combat, a multi-round fight auto-resolves under Uro Basic (a pure function of the seed — same seed → byte-identical replay; a seed-sweep test asserts the acceptance's PC-loses outcome holds across many seeds), and the lost fight leaves persistent consequences — the PC downed (hp 0), a `truth=true` injury claim, and a looted item (`ItemTransferred`) — visible in later free-roam and carried on a fork.
- **inc 3.1 (ruleset port + Uro Basic):** seeded `Rng`, the `Ruleset` Protocol (`rulesets/base`) + value types, and Uro Basic (`rulesets/uro_basic`) — d20 checks, the encounter state machine, deterministic NPC AI (D-26), effects, level 1-5 progression. A fight replays byte-identically from one seed. Review: 19 → 6 fixed.
- **inc 3.2 (planned free-roam pipeline, D-28):** the planner stage (schema-forced `BeatPlan`) + deterministic plan validation (affordance fence + D-21 trigger coverage), the mechanics gate (ruleset checks + a per-beat seeded `Rng`), character sheets as state (`SheetUpdated`, `proj_sheets`). No ruleset bound → unchanged Phase-1 flow. Review: 19 → 10 fixed.
- **inc 3.3 (encounter mode):** mode transitions (attack → encounter), the auto-resolved initiative loop (`pipeline/encounter.py`, D-29 — narrows D-26: both sides via `npc_action`, whole fight in one beat; interactive per-turn play deferred), effects→R-events→projections (`ActorDamaged`→hp, `ItemTransferred`→`proj_items`), lost-fight consequences. Review: 12 → 4 fixed (encounter now requires 2 distinct known opponents on opposing teams, else falls back to free-roam; D-29 doc-reconciliation).
- **Phase-end holistic review** (whole phase, 5 dims + completeness critic): 17 candidates → 8 fixed, **0 code defects** — the loot half went live (`ItemCreated` now emitted by `start_campaign` as starting gear, so a lost fight loots real items in play), and consequence-gating / "move+action" / the determinism claim were reconciled from overclaim to honest. (Per-increment reviews are now folded into this one phase-end pass — owner directive; see "How increments get built".)

**Phase 4 (worlds) is COMPLETE — code + a phase-end holistic review, 148 tests green, every acceptance leg demonstrated** (deterministic legs fully; the LLM legs — backfill/probe — stub-tested, live pass pending like Phases 1/3). The acceptance runs end-to-end across `worlds/ashfall` (rich → runnable) and `worlds/thornwood` (thin): `validate` flags Thornwood's missing conflict seed → `create --backfill` fills it and **commits it as a `ThreadCreated` tagged `provenance=ai_backfill`** (queryable in `proj_threads`) → `probe` passes → `seed 42` vs `seed 43` produce different dynasties/wars on identical authored geography → a campaign narrates in the pack's tone.
- **inc 4.1 (pack format):** `worldpack/{models,parse,sufficiency}` — Pydantic manifest+seed schemas, `parse_pack`, the sufficiency rubric (runnable|thin|insufficient + gaps). CLI `uro world validate`; two example packs.
- **inc 4.2 (import + seeding):** factions + the graph edge table (migration 010, deferred since P1) come due; `pack_to_events` (emitter S) commits authored geography at `WorldGenesis`; `engines/history.seed_history` procedurally generates dynasties/wars (emitter H) — pure fn of (manifest, seed), byte-identical replay, seed-varying. CLI `uro world create` / `uro world seed`.
- **inc 4.3 (prompt packs, D-6):** default Jinja2 templates in `uro_core/prompts/` + pack override-by-filename (`PromptEnv`); the manifest tone flows to the narrator (`WorldGenesis` stores tone+overrides; the engine loads per-branch `world_style`). The three stage prompts are now templates.
- **inc 4.4 (creator loop):** `worldpack/backfill` (opt-in AI gap-fill, provenance-tagged), `engines/probe` (structured_output gate + content_rating, judge-scored D-24, warn-not-fail), `Engine.preview_beat` (dry-run — full pipeline, no commit). CLI `uro world backfill` / `uro world probe` / `uro dry-run`.
- **Deferred (honest):** LLM lore-extraction (authored YAML is the primary source); backfill fills only the *conflict* dimension (others extend the same pattern); persisted/timestamped probe-report storage (PoC prints with transcripts); per-commit temporal edge validity (docs/07 — PoC does current-state edges); the rest of the docs/04 probe suite (context_window/instruction_following/consistency/latency) — same ask→judge pattern; the `TEMPLATE_API_VERSION` pin (constant reserved, not enforced).
- **Phase-end holistic review** (whole phase, 5 dims + completeness critic): 22 candidates → 18 confirmed → triaged. **Real seam fixed:** the backfill→import chain was severed (backfill print-only, threads never imported) so `provenance=ai_backfill` reached no event — now threads commit as `ThreadCreated` (emitter S, `proj_threads`, migration 011) and `create --backfill` wires backfill into import, making the acceptance's backfill leg real. Also: snapshot ordering now total (all columns, not first-two), sufficiency checks the runtime template name, `PromptEnv` is `StrictUndefined`. The rest were **doc-drift reconciled** in the same commit (docs 04/07/09/12/13/15: prompt-pack filenames, probe "stored report", emitter-whitelist framing, WorldGenesis payload, factions/edges/threads now Built, seeding-scale honesty).
**Phase 5 (server & sessions / federation) is code-complete, 162 tests green, fully deterministic (no key needed — all legs stub/fake-tested). All three acceptance legs are demonstrated:** (a) two clients on one campaign both receive the same streamed beats; (b) a world exports → verifies → imports → continues; (c) the **war-story test** — an external toy battle's feat propagates to surviving witnesses and a tavern NPC retells a garbled, traceable rumor; zero survivors → silence.
- **inc 5.1 (server + participants):** `uro_core/session.py` (Participant/Session + the `TurnArbiter` port + `SoloArbiter`); `uro-server` FastAPI shell over a `ServerDeps` seam (so transport is testable without a live DB) — `SessionHub` broadcast fan-out, WS `/campaigns/{c}/play` with token auth. CLI `uro serve` / `uro connect`.
- **inc 5.2 (export/import):** `uro_core/export.py` — `WorldBundle` + `verify_bundle` (recomputes the SHA-256 chain, `ExportError` on tamper); `store.export_world`/`import_world` (import re-instantiates a fresh world, remapped ids, projections rebuilt by replay). CLI `uro world export`/`import`. The export re-derives a canonical chain over the stored events (JSONB doesn't preserve key order, so the in-DB hash isn't byte-reproducible — the bundle is instead internally verifiable end-to-end).
- **inc 5.3 (belief propagation):** `engines/actor.propagate_belief` — a feat fans out along `knows` edges (BFS, per-hop confidence decay, first-hearer-wins), tier-agnostic, `learned_from` making the chain traceable to the witnesses. No witnesses → nothing propagates.
- **inc 5.4 (Chronicler mode, D-25):** `CausedBy.external` + `external_cause` (emitter E); `uro_core/chronicler.py` (`OutcomeBundle` v0 + `distill_outcome` — a feat commits as `truth=unknown` **testimony** that belief-propagates to witnesses, casualties→ActorDied, loot→ItemTransferred); `scripts/toy_battler.py` (the external "game"); server `POST /campaigns/{c}/encounters/{e}/outcome` (authed). **Recall fix:** an on-stage NPC's belief about an OFF-STAGE subject now surfaces (a `belief_claims` channel — rendered as beliefs, not scene facts), and belief **confidence** surfaces as certainty phrasing ("has heard a rumor" vs eyewitness) — so a garbled, low-confidence rumor is actually retold as one.
- **Phase-end holistic review** (whole phase, 5 dims + completeness critic): 23 candidates → 12 confirmed → triaged. **Real trust hole fixed:** the external tier minted `truth=true` canon from arbitrary bundle prose with no gauntlet, `E` wasn't whitelisted for `ClaimRecorded`, and the outcome endpoint had no auth — now a feat is `truth=unknown` **testimony** (an external bundle can't assert protected canon; scope enforced, gauntlet-on-interpretation deferred OQ-12), `E` is whitelisted for testimony, the endpoint requires a token, and belief `BeliefChanged` carries the Actor-service (A) cause. Also: the war-story distortion now **reaches the narrator** (confidence→phrasing), `verify_bundle` binds world-name/branches/markers (not just the event chain), `export_world` reads in one transaction. The rest was doc-drift reconciled (docs 10/12/13: E-tier/gauntlet honesty, the unbuilt in-flight-beat guard, "confidence decay" not "detail", keyless-integrity wording).
- **Deferred (honest):** the full REST management surface (docs/08 lists worlds/branches/chronicle/state/usage — the WS play channel + outcome endpoint ship, the rest are scaffolded by the CLI); `PartyArbiter` (OQ-7, the arbiter port is ready); the full Chronicler ingestion contract incl. gauntlet-on-feat-testimony (OQ-12); garbled-STATEMENT rumor distortion (PoC does confidence decay); the per-campaign in-flight/expected-head concurrency guard (single-writer by convention); persisted/timestamped probe reports.
- **All five PoC phases are complete + a final cross-phase review** (7 confirmed phase-interaction seams: memory dropped on export→import now carried like a fork; sheet-less `ActorDied` now leaves a `proj_actors.status` death trace, migration 012; README/docs drift).

## Post-PoC: LIVE-VALIDATED (2026-07-08, 170 tests green, on GitHub `cupskeee/uro` private)

The owner runs keyed `--provider openai` commands (their key isn't visible to Claude's bash); Claude analyzes from Postgres. Runbook: `docs/live-run.md`.

- **Thesis validated (honestly):** `scripts/ablation.sh` (clean 14/14 via `--no-mechanics`) — the FULL engine built real durable state (actors/claims/beliefs/memories); BARE built none, and past the recency window FULL re-surfaced the established NPC + world facts as KNOWN continuity while BARE re-introduced/drifted. **Honest caveat:** not a clean "bare forgot the name" story (both arms were name-consistent — independent generations); the real signal is state-building + recall-as-known.
- **Planner made reliable live** (was ~50% freeform failure → 0%): `parse_plan` salvages small-model near-misses; `validate_plan` governs only the ruleset's DECLARED trigger vocab (ignore invented "social"/"movement" — D-21 still enforced for real triggers); prompt constrains triggers. Added `uro play --no-mechanics` (full recall+extract, no planner; distinct from `--bare`).
- **Entity resolution: Layer 1 shipped, Layer 2 declined on data.** Canonical name matching (case/whitespace/article-folded) fixes the observed "the woman"/"woman" split; a live stress-test then showed ZERO splits, so the embedding `entity_index` (OQ-3, false-merge risk) was correctly NOT built — validating before building.
- **Open quality follow-ups (not fixed):** flavor over-extraction (atmosphere sometimes → `truth=true`); planner re-ask latency; the `entity_index` (deferred).

## Post-PoC Phase 6 — THE ALIEN RULESET is COMPLETE (2026-07-08, 189 tests green, deterministic — no key)

The first post-PoC phase, settling **OQ-13 → D-30**: the ruleset port is now **game-agnostic by construction**, proven by a deliberately non-d20 second built-in. Claude built AND verified fully (like Phases 2/3 — no owner key needed). **The acceptance PASSES** (`tests/test_alien_acceptance.py`): a PbtA campaign, bound via the registry, plays a conflict beat whose **7-9 partial success leaves a persistent, canonical consequence** (an `Exposed` condition / a filled harm clock) a binary d20 result cannot express — carried across a fork; d20 and PbtA coexist in one build with irreconcilable harm shapes (hp vs a 0-4 clock) through the identical runner. **Live-verified:** `uro world create worlds/emberfell` → a campaign bound to `uro-pbta` whose PC sheet is genuinely PbtA (`stats`/`harm`/`conditions`, no `hp`/`ac`).

- **A 7-surface leak audit found 64 d20 assumptions** in the "generic" port; building `uro_pbta` in lockstep forced every one out (full leak report in D-30).
- **inc 6.1 (port generalization, one cohesive slice — no 6.2):** `rulesets/base.py` names no game vocabulary — opaque `dict` sheets, ruleset-graded `CheckResult.outcome` (not binary `success`), no DC, opaque `EncounterState`, open action/effect kinds, harm via the ruleset's opaque final `SheetUpdated` (projector `{hp}` hardcodes deleted). `uro_basic` refactored to own its d20 shape (meteor + encounter replay byte-identical); `uro_pbta` added (2d6 vs 7/10, harm clock, moves, advance-by-failing).
- **inc 6.3 (registry & binding):** `rulesets/registry.py` resolves `id@version` → a bound `Ruleset`; `WorldGenesis`/`CampaignStarted` pin it (migration 013), `play`/`dry-run` rebind; `worlds/emberfell` is the PbtA example pack. (`serve` binds one ruleset/process — per-campaign rebind deferred, documented.)
- **Phase-end holistic review** (5 dims + completeness critic → 7 confirmed + 3 critic): **1 real fix** — the `ActorDamaged` projector handler was restored as documented **legacy replay-compat** (removing it broke rebuild-by-replay for pre-Phase-6 d20 logs; the current runner still uses opaque `SheetUpdated`). Also: removed the orphaned d20 `initiative` field from the shared `EncounterStarted`; `world validate`/`create` now catch an unregistered ruleset id; the rest was doc-drift reconciled (get_sheet/events/docs-12) + two accepted, named deferrals (bare-`ActorDied` leaves an inert stale sheet, `status='dead'` authoritative; `serve` one-ruleset).

## Post-PoC Phase 7 — MULTIPLAYER (PartyArbiter) is COMPLETE (2026-07-08, 206 tests green, deterministic)

Settles **OQ-7 → D-31**. Parallel to Phase 6: a single-player leak audit found **35** assumptions in the "multiplayer-shaped" seam (D-9), and building a real `PartyArbiter` forced them out. Claude built AND verified fully (no key). **Live-verified:** `uro campaign join` seats a 2nd participant on their own PC (two distinct `proj_pcs` bindings).

- **inc 7.1 — every beat runs as the ACTING participant's PC:** `pc_for_participant(campaign_id, participant_id)` (proj_pcs by participant, branch-scoped); `participant_id` threaded through `_context`→planner/mechanics/encounter (`campaign_pc` is now the solo fallback); `store.bind_pc` party-join primitive.
- **inc 7.2 — the PartyArbiter:** `TurnArbiter` widened `admit->bool` → `admit->AdmitDecision` (ADMITTED/NOT_YOUR_TURN/REJECTED; QUEUED reserved) + `note_joined`/`note_left`/`beat_committed` lifecycle; `PartyArbiter` = round-robin turn token over the connected roster (session-only turn state, D-31); `SoloArbiter` = degenerate. Server drives the lifecycle + `not_your_turn` broadcast; CLI `uro campaign join` + `uro serve` >1 token → PartyArbiter.
- **Phase-end SYSTEM-WIDE cross-phase review** (P7 × P0..P6 + invariants + arbiter-lifecycle, the standard now): **8 confirmed + 2 critic → all fixed.** The real ones: an unbound party participant could silently drive **another player's PC** via the solo fallback (now: 0 PCs → no-PC beat, 1 → solo fallback, ≥2 → refuse); **auto-resolved PvP** downed+looted another player's PC (now falls back to free-roam); `bind_pc --adopt` clobbering an active PC (rejected); a failed turn wedging the party (now rotates); + fork-scoping, roster refcount, narrator acting-PC. **Multiplayer touched more seams (10) than Phase 6 (3) — the cross-phase review earned its keep.**
- **Deferred (named):** proposal-window/consensus/GM-player arbiters (behind the same port; QUEUED reserved); auto-bind-PC on WS-join (`campaign join` pre-seats); `expected_head` optimistic-concurrency (round-robin serializes turns); consensual PvP; party co-combat + PC-anchored recall.

## Post-PoC Phase 8 — CHRONICLER INGESTION HARDENING is COMPLETE (2026-07-08, 215 tests green, deterministic)

Closes the trust gaps the Phase-5 toy proof (D-25) deliberately deferred → **D-32** (OQ-12 hardened, but the OQ stays open for the full contract). An adversarial "abuse the Chronicler" audit (5 vectors) found **25 real exploits** — worst: a single authed POST could kill or loot **any** actor (a PC, a T3 king) never in the fight, feats bypassed the gauntlet, no caps/dedup. Claude built AND verified fully (no key).

- **The hardening (`distill_outcome` is now a trust-scoped gate, reusing existing ports):** protection ceiling (a PC/`is_pc` or T2+ named actor can't be killed/looted/first-hand-witnessed → its casualty downgrades to `truth=unknown` testimony; only an unprotected T0/T1 **declared combatant** dies); participant scope (casualties/loot/feat.actor/witnesses ∈ `bundle.participants`); existence+ownership (loot needs a real item actually owned by `from_ref`); feat.actor entity-resolved; feats stay `truth=unknown` testimony; anti-abuse (bundle list caps + deterministic claim ids → idempotent replay + a `_item_transferred` projector ownership-guard).
- **Phase-end SYSTEM-WIDE cross-phase review** (P8 × P0..P7 + invariants): **1 confirmed (low), ZERO in the highest-risk seams** — the engine-wide `_item_transferred` guard is confirmed not to break P3 combat loot / P5 import / P2 fork-replay. The one fix: `distill_outcome` now passes the RAW feat.actor name to `find_actor_by_name` (matching the P1 extractor's exact-name tiebreak) instead of a pre-canonicalized form.
- **Deferred (named, "full contract waits for a real external game"):** a persisted parked-encounter registry (non-self-attested scope) + outcome-bundle schema-v1 + game↔world time mapping + finer endpoint→campaign authority + emitter-whitelist-at-append (the LLM path is already gauntlet-fenced; `distill_outcome` emits a fixed safe event set).

## Post-PoC Phase 9 — THE REACTION LAYER (pack modules) is COMPLETE (2026-07-10, 242 tests green, deterministic)

Reverses **D-6** → **D-33**: pack-authored reactive behavior, built as a DECLARATIVE reaction layer (a pack ships `rules.yaml`/`agendas.yaml` DATA, never code — so the from-scratch sandbox D-6 feared is met STRUCTURALLY, not built). Preceded by a design pass (4-approach judge panel + critic; the declarative approach won 8.67 unanimous over WASM/DSL/Python-hooks). Full design in `docs/17-reaction-layer.md`. Closes the OQ-8 *mechanism* (thread lifecycle + off-screen agendas were engine-less — only `ThreadCreated` projected).

- **inc 1 (hook + vocabulary):** `CausedByKind="module"` + `module_cause`; `ThreadStateChanged` event/projector (advances `proj_threads.state`; no migration — the column pre-existed); `Engine.react` post-beat hook in `_finish` (a separate `caused_by=module` beat), proven with one hardcoded rule before any DSL.
- **inc 2 (pack format):** `worldpack/rules.py` — the CLOSED grammar as Pydantic discriminated unions (a Condition tree + an Action union that STRUCTURALLY cannot name a mechanical/lethal/canon event = the trust fence); `rules_api_version` enforced; the pack carried INLINE in `WorldGenesis` (export/import self-contained).
- **inc 3 (interpreter + gauntlet):** `engines/rules.py` `evaluate_rules` (closed condition eval, node budget) + `engines/rules_gauntlet.py` `run_rules_gauntlet` (scope fence, never-mint, forced `truth=unknown`/`origin=module` testimony, caps, deterministic ids) — the D-32 bar on untrusted rules. `propagate_belief` gained a `caused_by` param (module beliefs carry `module`, not laundered as `agenda`).
- **inc 4 (downtime tick):** `Engine.agenda_tick` fires cadence-based agenda rules at `time_skip` (off-screen faction edges/rumors); wired into `uro branch fork --time-skip-days`.
- **Phase-end SYSTEM-WIDE cross-phase review** (P-reaction × P0..P8 + invariants): **3 confirmed + 1 high critic → all fixed.** The high one: the Chronicler outcome path bypassed `react` (and combat is `lethal=False`, so external death is the ONLY runtime `ActorDied`) — the flagship `ActorDied` rule was dead in real play; now `report_outcome` runs `react`. Also: `react`/`agenda_tick` made exception-isolated (a reaction fault never fails the already-durable beat, mirroring `_remember`); the `rules_api_version` pin moved onto the `RulePack` model (enforced at runtime + import, not just parse); docs/17 reconciled (the trigger-commit pin is behavioral-via-round-robin, structural pin is a future refinement).
- **Deferred (named):** deep off-screen SIMULATION (accumulating faction state — the grammar can't express counters; reserved sandbox tier's remit); a WASM scripting tier behind `ports/module.py` (refused until a computation-shaped use-case needs it; entity-minting is the sharpest trigger); the structural materialize-at-trigger-commit party-race pin; an append-time emitter whitelist (`caused_by=module` is the anchor).

## Dogfood: four games built ON the engine → an evidence-backed backlog (2026-07-12, 252 tests green)

`examples/hello_uro/` is the reference embedding consumer. Then **four games as forcing functions** (`examples/games/{ironwake,sable-court,seventh-vault,hollowloop}/`, each a `system_prompt.md`+`TASK.md` brief → a real built game + a `GAP_REPORT.md`), each aimed at a different cluster of deferred/by-policy/unproven work. The synthesis is **`docs/18-gap-findings.md`** (the prioritized backlog + the *validated deferrals* = what NOT to build).

- **4 confirmed findings landed immediately** (commit `3d82151` + migration 014): a fork hot-path `Seq Scan` on `memory_index` (missing `commit_id` index — 30-60% of every fork, found by a 500-fork game); a `PartyArbiter` holder-disconnect misrotation (off-by-one, masked by a cursor-0-only test); accepted-but-inert rule triggers (unknown event type / bad `where` key validated silently — hit by 3 games, now rejected at parse + `create_world`); silent rule-pack death (a bad pack was swallowed by INC-5's exception isolation — now `create_world` validates loudly).
- **Stage-B evidence gate has FIRED:** D-33 reserved the WASM/computation tier "until a refusal log shows authors need computation the DSL can't express." All 4 games produced one (Sable Court's 12 wished-for rules in exact syntax is the headline), plus a new argument — refused counters live in game code and so **fall out of `fork_branch`**, making the two flagship features undermine each other. Next decision: scope engine-owned numeric state, superseding the D-33 reservation.
- **Validated deferrals (killed speculative work):** a specialized graph/vector store is NOT justified (Postgres held flat to 500 forks); `entity_index` (OQ-3) is NOT justified (name+alias held, 0 false merges); the protection ceiling is a *feature*, not a defect.

Next: act on `docs/18` — B1 `append_and_react` (unanimous, small) and the Stage-B computation-tier decision are the top of the list; the rest of the **Post-PoC horizon** (docs/10) as before.
